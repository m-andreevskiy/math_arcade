
using TMPro;
using UnityEngine;

public class TextInfo : MonoBehaviour
{

    [SerializeField] private TextMeshPro text;
    [SerializeField] private ScriptableObject script;

    // Update is called once per frame
    void Update()
    {
        // text.text = FindAnyObjectByType;

    }
}
